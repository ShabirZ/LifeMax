import { TrendingUp, Shield, CheckCircle2, Clock } from "lucide-react";

export default function Features() {
  return (
    <section id="features" className="py-24 bg-[#050505]">
      {/* ... (Copy the Bento Grid code here) ... */}
       <div className="container mx-auto px-6">
          <div className="text-center mb-16">
            <h2 className="text-3xl md:text-4xl font-bold mb-4">The Unified Protocol</h2>
            <p className="text-gray-400 max-w-xl mx-auto">Most apps treat these categories as separate. LifeMax treats them as a single ecosystem.</p>
          </div>
          {/* Grid content... use the same code as before */}
          {/* If you want I can paste the full grid code again, but keeping it concise here! */}
       </div>
    </section>
  );
}